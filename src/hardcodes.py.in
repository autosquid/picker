# -*- coding: utf-8 -*-
import os

class Hardcodes(object):
    def __init__(self, args):
        "docstring"
        pass
    rootdir = "${projroot}"
    datadir = os.path.join(rootdir, "data")
    testdataroot = os.path.expanduser(datadir)
    ucalibdataroot = os.path.join(datadir, "Lobo")
    dockerdatavolumn_local = os.path.join(datadir, "mlsddata")
    dockerdatavolumn = "/data"
    dockeroutput_subdir = "output"
